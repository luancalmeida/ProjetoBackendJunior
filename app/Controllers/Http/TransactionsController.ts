

/*import type { HttpContextContract } from '@ioc:Adonis/Core/HttpContext'
import Transaction from 'App/Models/Transaction'
import UsersController from './UsersController'


export default class TransactionsController {

    public async storeTrasaction(req, res){
        const {value} = req.file;
        const{description , price, location, status} = req.body;
        const {user_id} = req.headers;

        const trasaction = await Trasaction.create({
            user: user_id,
            thumbnail: filename,
            description,
            price,
            location,
            status,


        })
    }

    public async getTrasactions({ params}: HttpContextContract){
        
        let transaction = await Transaction.find(params.accountId)
        return transaction;
        
    }

}
*/

    


        

